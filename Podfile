# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ActivityContainer' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ActivityContainer
  pod 'SnapKit', '~> 5.7.0' 
  target 'ActivityContainerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ActivityContainerUITests' do
    # Pods for testing
  end

end
